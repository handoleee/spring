<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 
 <mapper namespace="lm"> 
 	<select id="likelist" resultType="likedto">
 		SELECT*FROM LIKETB WHERE LID={#lid}
 	</select>
 	
 	<insert id="addlikelist" parameterType="likedto">
 		INSERT INTO LIKETB(LNUMBER, LBRANDNUM, LID) VALUES(LIK_SEQ.NEXTVAL, #{lbrandnum}, #{lid})	
 	</insert>
	<!-- 
	
	<update id="addlikelistprocess" parameterType="String">
		UPDATE LIKETB SET LNUMBER=#{lnumber}, LBRANDNUM=#{lbrandnum} WHERE LID=#{lid}
	</update> 
	
	</insert> -->
	
 </mapper>
 
 
 
 
 
 
 
 
 
 
 
 